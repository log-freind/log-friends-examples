name: Examples CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: log-friends-examples-deploy
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/log-freind/log-friends-examples
  K8S_NAMESPACE: log-friends
  K8S_DEPLOYMENT: log-friends-examples
  HEALTH_URL: http://127.0.0.1/examples/products

jobs:
  build-and-deploy:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Validate runner tools
        run: |
          docker version
          microk8s status --wait-ready

      - name: Test
        run: ./gradlew test --no-daemon

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Restart Examples deployment
        run: |
          microk8s kubectl rollout restart deployment/${K8S_DEPLOYMENT} -n ${K8S_NAMESPACE}
          microk8s kubectl rollout status deployment/${K8S_DEPLOYMENT} -n ${K8S_NAMESPACE} --timeout=180s

      - name: Health check
        run: |
          curl --fail --retry 12 --retry-delay 5 --retry-connrefused "${HEALTH_URL}"
